import { defineConfig } from 'vite'

export default defineConfig({
  // IMPORTANT: Change 'santhoshini-portfolio' to your actual GitHub repo name
  base: '/santhoshini-portfolio/',
  build: {
    outDir: 'dist',
  },
})
